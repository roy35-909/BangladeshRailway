from django.contrib import admin
from django.urls import path
from .views import *
urlpatterns = [
    # path('search_result/', login_view, name="LoginView "),
    # path('register/', register_view, name="RegisterView "),

]